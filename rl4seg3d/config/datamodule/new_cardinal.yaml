_target_: rl4seg3d.datamodules.RL_3d_datamodule.RL3dDataModule
data_dir: /home/local/USHERBROOKE/juda2901/dev/DELETE_ME/CARDINAL_234CH_FINAL/
num_workers: 12
pin_memory: True
batch_size: 1 # must be 1
dataset_name: ""
csv_file: cardinal.csv
splits_column: split_approved # column for train-test-val split, will be created if doesn't yet exist
seed: ${seed}

max_image_area: null
common_spacing: [0.37, 0.37, 1] # approx average spacing in dataset
max_window_len: 4
max_batch_size: 1
max_tensor_volume: 6500000 # alright for 24gb gpu (NOT IN RL USE CASE)
shape_divisible_by: [32, 32, 4]

approx_gt_dir: ${logger.save_dir}
